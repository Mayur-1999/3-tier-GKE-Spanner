name: Service Mesh Build
on:
  workflow_dispatch:
  push:
    branches: 
      - Dev
      - Test
      - Prod
    paths:
      - istio-1.20.2/**
      - .github/workflows/servicemesh_ci.yaml
jobs:
  Istio-Artifact:
    runs-on: ubuntu-latest    
    defaults:
      run:
        shell: bash
        working-directory: /home/runner/work/3-tier-GKE-Spanner/3-tier-GKE-Spanner/istio-1.20.2
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: List Content
        run: |
            ls 
  
      - uses: actions/checkout@v3
      - name: Echo
        run: echo "Creating artifact"
        
      - uses: actions/upload-artifact@main
        name: Uploading Istio Artifact
        with:
          name: Istio-Artifacts
          path: ./istio-1.20.2/*
