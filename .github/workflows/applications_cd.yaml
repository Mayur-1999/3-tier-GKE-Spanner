name: Helm Deploy
on:
  workflow_dispatch:
    inputs:
      app_name:
        description: "App Name - adservice, cartservice, checkoutservice, currencyservice, emailservice, paymentservice, productcatalogservice, recommendationservice, shippingservice"
        required: True
      helm_command:
        description: "install/upgrade/rollback"
        required: True
env:
    app_name: ${{ inputs.app_name }}
    helm_command: ${{ inputs.helm_command}}

jobs:
  Verify-Deployment-Dev:
    name: 'Verify Dev Deployment: ${{ inputs.app_name }}'
    runs-on: self-hosted-runner
    defaults:
      run:
        shell: bash
    if: github.ref == 'refs/heads/Dev'
    environment:
     name: Dev_Env
    steps:

    - name: Download Artifacts
      uses: dawidd6/action-download-artifact@v8
      with:
        workflow: applications_ci.yaml
        github_token: ${{ github.token }}
        repo: ${{ github.repository }}
        name: Application-Artifacts
        path: Application-Artifacts
    
    - name: Environment
      run: |
          echo "${{ vars.PROJECT_ID }}"
          echo "${{ vars.GKE_CLUSTER_NAME }}"

    - name: Application ${{ inputs.app_name }}
      run: |
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            echo "helm install ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            echo "helm upgrade ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            echo "helm rollback ${{ inputs.app_name }} --debug --dry-run VERSION_NUMBER"
          else 
            echo "Wrong input" 
          fi 

    - name: Install Helm
      run: |
          curl https://baltocdn.com/helm/signing.asc | gpg --dearmor | sudo tee /usr/share/keyrings/helm.gpg > /dev/null
          sudo apt-get install apt-transport-https --yes
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/helm.gpg] https://baltocdn.com/helm/stable/debian/ all main" | sudo tee /etc/apt/sources.list.d/helm-stable-debian.list
          sudo apt-get update
          sudo apt-get install helm -y 
          helm version
    
    - name: Setup Gcloud CLI
      uses: google-github-actions/setup-gcloud@94337306dda8180d967a56932ceb4ddcf01edae7
      with:
        service_account_key: ${{ secrets.GKE_SA_KEY }}
        project_id: ${{ vars.PROJECT_ID }}  

    - name: GKE-Gcloud Auth setup
      run: |
          gcloud --quiet auth configure-docker
          gcloud components install gke-gcloud-auth-plugin
          gke-gcloud-auth-plugin --version
          gcloud config set project "${{vars.PROJECT_ID}}"
          gcloud container clusters get-credentials "${{vars.GKE_CLUSTER_NAME}}" --region="${{vars.GKE_ZONE}}"

    - name: ${{ inputs.helm_command}} ${{ inputs.app_name }} Application
      run: |
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            helm install ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            helm upgrade ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            helm rollback ${{ inputs.app_name }} --debug --dry-run VERSION_NUMBER
          else 
            echo "Wrong input" 
          fi 


  Verify-Deployment-Test:
    name: 'Verify Test Deployment: ${{ inputs.app_name }}'
    runs-on: self-hosted-runner
    defaults:
      run:
        shell: bash
    if: github.ref == 'refs/heads/Test'
    environment:
     name: Test_Env
    steps:

    - name: Download Artifacts
      uses: dawidd6/action-download-artifact@v8
      with:
        workflow: applications_ci.yaml
        github_token: ${{ github.token }}
        repo: ${{ github.repository }}
        name: Application-Artifacts
        path: Application-Artifacts
    
    - name: Environment
      run: |
          echo "${{ vars.PROJECT_ID }}"
          echo "${{ vars.GKE_CLUSTER_NAME }}"

    - name: Application ${{ inputs.app_name }}
      run: |
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            echo "helm install ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            echo "helm upgrade ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            echo "helm rollback ${{ inputs.app_name }} --debug --dry-run VERSION_NUMBER"
          else 
            echo "Wrong input" 
          fi 

    - name: Install Helm
      run: |
          curl https://baltocdn.com/helm/signing.asc | gpg --dearmor | sudo tee /usr/share/keyrings/helm.gpg > /dev/null
          sudo apt-get install apt-transport-https --yes
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/helm.gpg] https://baltocdn.com/helm/stable/debian/ all main" | sudo tee /etc/apt/sources.list.d/helm-stable-debian.list
          sudo apt-get update
          sudo apt-get install helm -y 
          helm version
    
    - name: Setup Gcloud CLI
      uses: google-github-actions/setup-gcloud@94337306dda8180d967a56932ceb4ddcf01edae7
      with:
        service_account_key: ${{ secrets.GKE_SA_KEY }}
        project_id: ${{ vars.PROJECT_ID }}  

    - name: GKE-Gcloud Auth setup
      run: |
          gcloud --quiet auth configure-docker
          gcloud components install gke-gcloud-auth-plugin
          gke-gcloud-auth-plugin --version
          gcloud config set project "${{vars.PROJECT_ID}}"
          gcloud container clusters get-credentials "${{vars.GKE_CLUSTER_NAME}}" --region="${{vars.GKE_ZONE}}"

    - name: ${{ inputs.helm_command}} ${{ inputs.app_name }} Application
      run: |
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            helm install ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            helm upgrade ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            helm rollback ${{ inputs.app_name }} --debug --dry-run VERSION_NUMBER
          else 
            echo "Wrong input" 
          fi 


  Verify-Deployment-Prod:
    name: 'Verify Prod Deployment: ${{ inputs.app_name }}'
    runs-on: self-hosted-runner
    defaults:
      run:
        shell: bash
    if: github.ref == 'refs/heads/Prod'
    environment:
     name: Prod_Env
    steps:

    - name: Download Artifacts
      uses: dawidd6/action-download-artifact@v8
      with:
        workflow: applications_ci.yaml
        github_token: ${{ github.token }}
        repo: ${{ github.repository }}
        name: Application-Artifacts
        path: Application-Artifacts
    
    - name: Environment
      run: |
          echo "${{ vars.PROJECT_ID }}"
          echo "${{ vars.GKE_CLUSTER_NAME }}"

    - name: Application ${{ inputs.app_name }}
      run: |
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            echo "helm install ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            echo "helm upgrade ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            echo "helm rollback ${{ inputs.app_name }} --debug --dry-run VERSION_NUMBER"
          else 
            echo "Wrong input" 
          fi 
    
    - name: Install Helm
      run: |
          curl https://baltocdn.com/helm/signing.asc | gpg --dearmor | sudo tee /usr/share/keyrings/helm.gpg > /dev/null
          sudo apt-get install apt-transport-https --yes
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/helm.gpg] https://baltocdn.com/helm/stable/debian/ all main" | sudo tee /etc/apt/sources.list.d/helm-stable-debian.list
          sudo apt-get update
          sudo apt-get install helm -y 
          helm version
    
    - name: Setup Gcloud CLI
      uses: google-github-actions/setup-gcloud@94337306dda8180d967a56932ceb4ddcf01edae7
      with:
        service_account_key: ${{ secrets.GKE_SA_KEY }}
        project_id: ${{ vars.PROJECT_ID }}  

    - name: GKE-Gcloud Auth setup
      run: |
          gcloud --quiet auth configure-docker
          gcloud components install gke-gcloud-auth-plugin
          gke-gcloud-auth-plugin --version
          gcloud config set project "${{vars.PROJECT_ID}}"
          gcloud container clusters get-credentials "${{vars.GKE_CLUSTER_NAME}}" --region="${{vars.GKE_ZONE}}"

    - name: ${{ inputs.helm_command}} ${{ inputs.app_name }} Application
      run: |
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            helm install ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            helm upgrade ${{ inputs.app_name }} --debug --dry-run Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            helm rollback ${{ inputs.app_name }} --debug --dry-run VERSION_NUMBER
          else 
            echo "Wrong input" 
          fi 

  Application-Deployment-Dev:
    needs: Verify-Deployment-Dev
    name: '${{ inputs.helm_command }}: ${{ inputs.app_name }}'
    runs-on: self-hosted-runner
    defaults:
      run:
        shell: bash
    if: github.ref == 'refs/heads/Dev'
    environment:
     name: Dev_Env
    steps:
    - name: Download Artifacts
      uses: dawidd6/action-download-artifact@v8
      with:
        workflow: applications_ci.yaml
        github_token: ${{ github.token }}
        repo: ${{ github.repository }}
        name: Application-Artifacts
        path: Application-Artifacts

    - name: Environment
      run: |
          echo "${{ vars.PROJECT_ID }}"
          echo "${{ vars.GKE_CLUSTER_NAME }}"

    - name: Application ${{ inputs.app_name }}
      run: |   
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            echo "helm install ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            echo "helm upgrade ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            echo "helm rollback ${{ inputs.app_name }} VERSION_NUMBER"
          else 
            echo "Wrong input" 
          fi 
          
    - name: Install Helm
      run: |
          curl https://baltocdn.com/helm/signing.asc | gpg --dearmor | sudo tee /usr/share/keyrings/helm.gpg > /dev/null
          sudo apt-get install apt-transport-https --yes
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/helm.gpg] https://baltocdn.com/helm/stable/debian/ all main" | sudo tee /etc/apt/sources.list.d/helm-stable-debian.list
          sudo apt-get update
          sudo apt-get install helm -y 
          helm version
    
    - name: Setup Gcloud CLI
      uses: google-github-actions/setup-gcloud@94337306dda8180d967a56932ceb4ddcf01edae7
      with:
        service_account_key: ${{ secrets.GKE_SA_KEY }}
        project_id: ${{ vars.PROJECT_ID }}  

    - name: GKE-Gcloud Auth setup
      run: |
          gcloud --quiet auth configure-docker
          gcloud components install gke-gcloud-auth-plugin
          gke-gcloud-auth-plugin --version
          gcloud config set project "${{vars.PROJECT_ID}}"
          gcloud container clusters get-credentials "${{vars.GKE_CLUSTER_NAME}}" --region="${{vars.GKE_ZONE}}"

    - name: ${{ inputs.helm_command}} ${{ inputs.app_name }} Application
      run: |
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            helm install ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            helm upgrade ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            helm rollback ${{ inputs.app_name }} VERSION_NUMBER
          else 
            echo "Wrong input" 
          fi 


  Application-Deployment-Test:
    needs: Verify-Deployment-Test
    name: '${{ inputs.helm_command }}: ${{ inputs.app_name }}'
    runs-on: self-hosted-runner
    defaults:
      run:
        shell: bash
    if: github.ref == 'refs/heads/Test'
    environment:
     name: Test_Env
    steps:
    - name: Download Artifacts
      uses: dawidd6/action-download-artifact@v8
      with:
        workflow: applications_ci.yaml
        github_token: ${{ github.token }}
        repo: ${{ github.repository }}
        name: Application-Artifacts
        path: Application-Artifacts

    - name: Environment
      run: |
          echo "${{ vars.PROJECT_ID }}"
          echo "${{ vars.GKE_CLUSTER_NAME }}"

    - name: Application ${{ inputs.app_name }}
      run: |   
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            echo "helm install ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            echo "helm upgrade ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            echo "helm rollback ${{ inputs.app_name }} VERSION_NUMBER"
          else 
            echo "Wrong input" 
          fi 
          
    - name: Install Helm
      run: |
          curl https://baltocdn.com/helm/signing.asc | gpg --dearmor | sudo tee /usr/share/keyrings/helm.gpg > /dev/null
          sudo apt-get install apt-transport-https --yes
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/helm.gpg] https://baltocdn.com/helm/stable/debian/ all main" | sudo tee /etc/apt/sources.list.d/helm-stable-debian.list
          sudo apt-get update
          sudo apt-get install helm -y 
          helm version
    
    - name: Setup Gcloud CLI
      uses: google-github-actions/setup-gcloud@94337306dda8180d967a56932ceb4ddcf01edae7
      with:
        service_account_key: ${{ secrets.GKE_SA_KEY }}
        project_id: ${{ vars.PROJECT_ID }}  

    - name: GKE-Gcloud Auth setup
      run: |
          gcloud --quiet auth configure-docker
          gcloud components install gke-gcloud-auth-plugin
          gke-gcloud-auth-plugin --version
          gcloud config set project "${{vars.PROJECT_ID}}"
          gcloud container clusters get-credentials "${{vars.GKE_CLUSTER_NAME}}" --region="${{vars.GKE_ZONE}}"

    - name: ${{ inputs.helm_command}} ${{ inputs.app_name }} Application
      run: |
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            helm install ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            helm upgrade ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            helm rollback ${{ inputs.app_name }} VERSION_NUMBER
          else 
            echo "Wrong input" 
          fi 

  Application-Deployment-Prod:
    needs: Verify-Deployment-Prod
    name: '${{ inputs.helm_command }}: ${{ inputs.app_name }}'
    runs-on: self-hosted-runner
    defaults:
      run:
        shell: bash
    if: github.ref == 'refs/heads/Prod'
    environment:
     name: Prod_Env
    steps:
    - name: Download Artifacts
      uses: dawidd6/action-download-artifact@v8
      with:
        workflow: applications_ci.yaml
        github_token: ${{ github.token }}
        repo: ${{ github.repository }}
        name: Application-Artifacts
        path: Application-Artifacts

    - name: Environment
      run: |
          echo "${{ vars.PROJECT_ID }}"
          echo "${{ vars.GKE_CLUSTER_NAME }}"

    - name: Application ${{ inputs.app_name }}
      run: |   
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            echo "helm install ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            echo "helm upgrade ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}"
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            echo "helm rollback ${{ inputs.app_name }} VERSION_NUMBER"
          else 
            echo "Wrong input" 
          fi 
          
    - name: Install Helm
      run: |
          curl https://baltocdn.com/helm/signing.asc | gpg --dearmor | sudo tee /usr/share/keyrings/helm.gpg > /dev/null
          sudo apt-get install apt-transport-https --yes
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/helm.gpg] https://baltocdn.com/helm/stable/debian/ all main" | sudo tee /etc/apt/sources.list.d/helm-stable-debian.list
          sudo apt-get update
          sudo apt-get install helm -y 
          helm version
    
    - name: Setup Gcloud CLI
      uses: google-github-actions/setup-gcloud@94337306dda8180d967a56932ceb4ddcf01edae7
      with:
        service_account_key: ${{ secrets.GKE_SA_KEY }}
        project_id: ${{ vars.PROJECT_ID }}  

    - name: GKE-Gcloud Auth setup
      run: |
          gcloud --quiet auth configure-docker
          gcloud components install gke-gcloud-auth-plugin
          gke-gcloud-auth-plugin --version
          gcloud config set project "${{vars.PROJECT_ID}}"
          gcloud container clusters get-credentials "${{vars.GKE_CLUSTER_NAME}}" --region="${{vars.GKE_ZONE}}"

    - name: ${{ inputs.helm_command}} ${{ inputs.app_name }} Application
      run: |
          if [[ ${{ inputs.helm_command}} == "install" ]]
          then 
            helm install ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "upgrade" ]]
          then
            helm upgrade ${{ inputs.app_name }} Application-Artifacts/Helm-Charts/${{ inputs.app_name }}
          elif [[ ${{ inputs.helm_command}} == "rollback" ]]
          then
            helm rollback ${{ inputs.app_name }} VERSION_NUMBER
          else 
            echo "Wrong input" 
          fi 
